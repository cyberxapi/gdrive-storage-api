# Google Drive Storage API - Environment Variables
# Copy this file to .env and fill in your values

# Google Drive API Credentials (JSON format as string)
# Get this from Google Cloud Console -> Service Account
GOOGLE_CREDENTIALS={"type": "service_account", "project_id": "your-project-id", ...}

# API Key for accessing the endpoints
# Use this key in query parameters: ?api_key=YOUR_API_KEY
API_KEY=your-secret-api-key-here

# Server configuration
PORT=8000
HOST=0.0.0.0

# Optional: Environment
ENVIRONMENT=production
